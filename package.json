{
  "name": "web-scraper-node",
  "version": "1.0.0",
  "description": "A CLI app that parses html pages and saves the content to a file",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^0.20.0",
    "request": "^2.69.0"
  }
}
